import { ObtenerEstadoPipe } from './obtener-estado.pipe';

describe('ObtenerEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new ObtenerEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
