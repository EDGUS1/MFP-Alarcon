import { ObtenerVotosPipe } from './obtener-votos.pipe';

describe('ObtenerVotosPipe', () => {
  it('create an instance', () => {
    const pipe = new ObtenerVotosPipe();
    expect(pipe).toBeTruthy();
  });
});
